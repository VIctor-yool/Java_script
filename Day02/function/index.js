// 함수: 마술상자

const a1 = function (x) {
  return x + 2;
};
a1(2); // 4

function abc(x) {
  return x + 10;
}
abc(20); // 30

//input[매개변수/파라메터]
function def(x, y) {
  return x * y;
}

const a = def(1, 5); // 5
def(2, 10); // 20
