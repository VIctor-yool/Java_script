const add = (x, y) => x + y;
const minus = (x, y) => x - y;
const multiply = (x, y) => x * y;
const double = (x) => x * 2;

function add1() {
  return x + y;
}
const add12 = () => {
  return x + y;
};
const add13 = () => x + y;
